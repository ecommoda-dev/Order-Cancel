<div dir="rtl" style="text-align: right;">

# إلغاء الأوردرات يدويًا (`Order-Cancel`)

![version](https://img.shields.io/badge/version-v2.1.0-blue)

**بتعمل إيه:** خدمة العملاء بتلغي أوردر قبل الشحن من غير ما تدخل داشبورد شوبيفاي، مع فرض شروط تمنع الإلغاء في الحالة أو الوقت الغلط.
**مين بيستخدمها:** خدمة العملاء (CS)
**الإصدار:** Worker `v2.9.0` · الواجهة `v1.9.0`   ← الاتنين مستقلين، طبيعي يختلفوا

## الروابط

```
الواجهة    : https://ecommoda-dev.github.io/Order-Cancel/
الـ Worker : https://order-cancel-worker.ecommoda-dev.workers.dev
اسم الـ Worker في الداشبورد: order-cancel-worker     ← لازم يطابق name في wrangler.toml
```

⚠️ **الاسم `orders-cancel-worker` (بـ s) غلط ومنتشر** — مذكور كده في
`ecommoda-constants` §11 بند 10 وكان مكتوب في تشينجلوج الواجهة. الاسم الحقيقي
في الداشبورد **`order-cancel-worker`** (اتأكد من `workers_list` يوم 01-09-2026).
أي `wrangler.toml` بالاسم الغلط = **Worker شبح** (فخ §7 التاني في سكيل النقل).

## الـ Endpoints

| `?action=` | بيعمل إيه |
|---|---|
| `lookup_order` | بحث بالرقم/`#`الرقم/**الـ Order ID الطويل**/رابط الأدمن + قائمة أسباب الإلغاء الحية |
| `cancel_order` | إلغاء + restock + كتابة السبب + انتظار الـ Job والتحقق من `cancelledAt` + **تحديث حالة S1 لـ `Cancelled`** |
| `diag` | فحص صلاحيات شوبيفاي (`write_orders` · `read_returns` · `write_returns`) |
| `get_config` | رقم نسخة الـ Worker — الواجهة بتقارنه بنسختها |
| `get_employees` | قائمة الموظفين النشطين — شاشة الدخول |
| `check_employee` · `register_pin` · `verify_employee` · `log_logout` | Universal D1 Auth |
| `get_logs` | صفحة من السجل (100 صف) — فلترة server-side: موظفين · أنواع · **أسباب** · بحث · فترة |
| `get_logs_count` | عدد الصفوف المطابقة — شبشة "النتائج" + الترقيم |
| `get_logs_export` | كل المطابق لحد 2000 صف + `cap`/`total`/`truncated` — للتصدير بس |
| `get_log_reasons` | أسباب الإلغاء الموجودة في السجل (`DISTINCT` من D1) — بتملا فلتر "سبب الإلغاء" |

## D1

```
tool  : order_cancel
type  : cancel · cancel_failed · login · logout
```

**ومن v2.6.0 الأداة بتكتب صف تاني كمان مع كل تغيير حالة:**

```
tool  : metafields_change          ← مسجّل في ecommoda-constants §7 (جدول الكُتّاب، v1.4.3)
type  : update
value_before / value_after : الحالة قبل وبعد
```

السبب: `ecommoda-order-lifecycle` قاعدة 9 — السجل ده هو **المصدر الوحيد** لأي
KPI عن زمن الدورة أو عدد المحاولات، وكتابة ناقصة = فجوة دائمة في الأرقام دي.

`extra` في صفوف `cancel` / `cancel_failed` بيشيل كمان (من v2.3.0):
`warehouseNotified` (إقرار الموظف إنه بلّغ الشحن/المخزن) و`fulfillmentStatusBefore`،
ومن v2.4.0 كمان `verify { jobDone, attempts, waitedMs }`، ومن v2.5.0 `result`
(`"success"` · `"warning"` · `"error"` — عمود "النتيجة" في تاب السجل بيقراه)،
ومن v2.6.0 `s1` (نتيجة تحديث الحالة) و`actions[]` (اللي تم فعليًا خطوة خطوة).

✅ **دوال السجل في `§SHARED` رجعت مطابقة للنسخة القياسية** في
`ecommoda-worker-builder` → `references/shared-functions.md` بعد ما المهارة
تبنّت الامتداد في **v2.0.0**: `buildLogFilterSQL(select, filters)` +
`employees[]`/`types[]` + `dateFrom`/`dateTo` + `logParamsFrom(url, tool)`
بقوا جزء من النموذج نفسه، والقيمة المفردة (`employee`/`type`) لسه مقبولة
للتوافق الرجوعي.

⚠️ **الفرق الوحيد الباقي عن النسخة القياسية:** باراميتر `reasons` — فلترة
بحقل متخزّن جوّه `extra` كـ JSON (`json_extract(extra,'$.reasonLabel')`)،
لأن سبب الإلغاء **مش عمود مستقل** في `logs`. ده لسه امتداد محلي (نص البند ٣
في `SKILL-FIXES.md`)، ومعلّم صراحةً بتعليق فوق الكتلة في `index.js`.

> ✅ **`cancel_failed` بقت مسجّلة في `ecommoda-constants` §7** — من **v1.4.3**
> الصف بقى `cancel · cancel_failed · login · logout`. كانت مخالفة قديمة
> (القيمة بتتكتب في D1 من v1.0.3) واتقفلت.
> ⚠️ ومعاها في نفس الإصدار: `metafields_change` بقى ليه **جدول كُتّاب معروفين**
> في §7، ومكتوب فيه إن مفتاح الإسناد **مش موحّد** — سكانرات بوسطة بتستخدم
> `extra.sourceTool` والأداة دي بتستخدم `extra.source`. أي جرد للصف ده لازم
> يقرا المفتاحين.

## المضبوط فعليًا في الداشبورد

> اللي **متظبط بالفعل** — مش اللي المفروض يكون. لسه محتاج تأكيد بالعين
> بعد أول ربط.

```
Bindings : DB → ecommoda-dev-logs
Secrets  : WORKER_SECRET · CLIENT_ID · CLIENT_SECRET
Vars     : SHOP_DOMAIN                      ← من [vars] في wrangler.toml
Build watch paths : index.js · wrangler.toml   ← اتضيّقت 01-09-2026 ✅
                    (صندوقين منفصلين — Exclude فاضية)
Production branch : main · Builds for non-production branches: مفعّلة
Deploy command    : npx wrangler deploy · Version: npx wrangler versions upload
```

**النسخة المنشورة وقت آخر تحديث للملف ده:** Worker `v2.8.0` (من دمج PR #3،
commit `40391a0`) — **متأكَّدة بقراءة الكود المنشور نفسه عبر MCP** يوم
01-09-2026، مش من الداشبورد بالعين: `WORKER_VERSION = "2.8.0"` و
`handleGetLogReasons` موجودة في الـ bundle المنشور.
⏳ **`v2.9.0` (مزامنة المهارات) لسه ما اتأكّدش منشور** — التأكيد بيتعمل بعد
الدمج بقراءة `WORKER_VERSION` من الكود المنشور عبر MCP، مش من الداشبورد بالعين.

**تصنيف الـ `env.*` (سكيل النقل §4-أ-٢):**

| المتغيّر | التصنيف | إزاي نتأكد |
|---|---|---|
| `WORKER_SECRET` · `CLIENT_ID` · `CLIENT_SECRET` | سر | قيمته مستحيلة القراءة — من مصدر أحمد |
| `SHOP_DOMAIN` | var بيرمي لو غاب | غيابه بيدّي `"error code: 1003" is not valid JSON` |
| — | var ليه fallback | **مفيش ولا واحد** ✅ يعني مفيش خطر أرقام غلط بصمت |

## CORS

`ALLOWED_ORIGINS` صارمة — `https://ecommoda-dev.github.io` بس. الأداة **بتكتب
وبتلغي أوردرات** (Option B). الدومين المهجور `ecommoda24.github.io` كان موجود
في v1.0.3 و**اتشال** — ده بيقفل جزء من `ecommoda-constants` §11 بند 10.

## خط الأساس بعد النقل

**أول تشغيل حقيقي بواجهة: 01-09-2026** (بند ١٠ في قائمة التحقق اتقفل).
الأرقام دي متأكَّدة من **D1** (`tool = order_cancel`) ومن **شوبيفاي** مباشرة.

```
دخول            : 06:33:05Z — Ahmed_Ibraheem
إلغاء #53033    : 07:51:25Z — id 7186876793154 — السبب: "تأجيل غير محدد"
إلغاء #53032    : 08:04:24Z — id 7186861523266 — السبب: "السعر غالي"
```

**حالة الأوردر على شوبيفاي بعد الإلغاء** (متأكَّدة على `#53033`):

```
cancelledAt              : 2026-09-01T07:51:27Z
cancelReason             : OTHER                       ← ثابت بالتصميم
displayFinancialStatus   : PENDING → VOIDED            ← بيتغيّر
displayFulfillmentStatus : UNFULFILLED (ما اتغيّرش)
custom.manual_status     : "Cancelled" بعد 15 ثانية    ← مش من الأداة، Flow
```

⚠️ **`#53032` أحمد مسحه من شوبيفاي بعد الإلغاء** (كان أوردر تجريبي ببيانات وهمية).
`node(gid://shopify/Order/7186861523266)` بيرجّع `null` والرقم مفقود من التسلسل
بين `#53031` و`#53033`. صف D1 بتاعه باقي — **الأثر الوحيد الباقي عليه**.

🔬 **الدليل الرقمي على باج التحقق (اللي اتصلح في Worker v2.4.0):**

```
07:51:25Z      الميتافيلد اتكتب (يعني الميوتيشن نجحت)
07:51:26.084Z  الـ Worker قرا الأوردر، لقى cancelledAt = null، وسجّل confirmed:false
07:51:27Z      شوبيفاي سجّلت cancelledAt فعليًا
               ↑ التأكيد كان على بُعد ثانية واحدة من لحظة ما الأداة استسلمت
```

الـ `job` في صف D1 راجع بـ `done:false` في الحالتين. النسخة الجديدة أول
انتظار فيها 400ms وبتكمل لحد ≈6 ثوانٍ — يعني كانت هتمسك الحالتين دول.

### تأكيدات إنتاج بعد أول نشر كامل (01-09-2026)

الواجهة والـ Worker اتنشروا مع بعض من `main` بعد دمج PR #1، والتالي **اتشاف
لايف** مش في بيئة اختبار:

- **الواجهة `v1.6.0`** شغّالة من `ecommoda-dev.github.io/Order-Cancel/` وتاب
  السجل بيقرا من D1 فعلاً (٣ صفوف — مطابق للعدّ من القاعدة).
- **حارس النسخة عدّى أول اختبار حقيقي له:** الهيدر **مافيهوش** تحذير
  "الـ Worker نسخة قديمة" — يعني `cmpVersion('2.7.0', '2.7.0') >= 0` والحارس
  ساكت في الوضع الطبيعي. ده الاتجاه السلبي من الاختبارين، والإيجابي كان
  اتجرّب في المتصفح قبل النشر (`1.0.3` → التحذير يظهر).
- **عمود "النتيجة" بيعرض "—" على الصفوف التلاتة** — وده **الصح**: التلاتة
  اتكتبوا قبل Worker v2.5.0 فمالهمش `extra.result`. السلوك الموثّق شغّال.

## فخاخ الأداة دي

- 🔴 **الـ Worker كان ناقصه `get_employees` تمامًا** لحد v2.1.0. الواجهة
  بتناديه في `loadEmployeesList()`، والـ Worker كان بيرد 404 → **مفيش موظف
  يقدر يدخل**. لو حصل rollback لأي نسخة قبل v2.1.0، الأداة بتتعطّل بالكامل
  مع إن كل حاجة تانية سليمة.
- ✅ **حارس نسخة الـ Worker بقى حد أدنى بدل تطابق حرفي — اتقفل 01-09-2026.**
  النمط القديم كان بيقارن `TOOL_VERSION` برقم الـ Worker تطابقًا حرفيًا،
  والرقمين **مستقلين بالتصميم** (سكيل النقل §12) — فالتحذير كان مولّع على طول
  والموظف بيتعوّد يتجاهله. دلوقتي `MIN_WORKER_VERSION = '2.5.0'` +
  `cmpVersion()` مقارنة رقمية، والتحذير (`verStaleBtn`) يولّع بس لو الـ Worker
  **أقدم** من الحد الأدنى. اتجرّب في الاتجاهين: 2.5.0 و9.9.9 و10.0.0 → مفيش
  تحذير · 1.0.3 → التحذير يظهر برسالته.
  ⚠️ **صيانة:** أي endpoint جديد الصفحة تعتمد عليه لازم يرفع `MIN_WORKER_VERSION`
  في **نفس التسليم** — لو اتنسي، الحارس بيرجع لوضع "مفيش حماية".
- ✅ **مفيش تصنيف أسباب خالص — كل إلغاء بيترفع لشوبيفاي بـ `OTHER`.**
  `REASON_ENUM_MAP` اتشال بالكامل في v2.2.0 (قرار أحمد 01-09-2026) بعد
  مراجعة كاملة للـ14 سبب. الثابت الوحيد دلوقتي `SHOPIFY_CANCEL_REASON`.
  **الفايدة:** مفيش انحراف صامت — أي سبب جديد على شوبيفاي بيشتغل فورًا
  من غير أي تعديل في الكود، والسلوك متوقع دايمًا.
  **التكلفة:** تقرير أسباب الإلغاء في شوبيفاي كله "Other" بلا تقسيم —
  أي تحليل لأسباب الإلغاء مصدره `custom.cancel_manual_reason` أو D1
  (`extra.reasonLabel`)، **مش** تقارير شوبيفاي.
- ✅ **الرقم المجرّد بيتجرّب مرتين، مش مرة.** `parseOrderInput` بترجّع **قايمة
  محاولات** مش محاولة واحدة: رقم ≥ 10 خانات بيتجرّب كـ Order ID الأول وبعدين
  كاسم أوردر، والأقصر بالعكس. قبل v2.3.0 أي رقم مجرّد كان بيتعامل كاسم أوردر
  بس، فالـ Order ID (`7186861523266`) كان بيرجّع "لم يتم العثور على الأوردر"
  والرابط الكامل بس هو اللي بيشتغل — لأن regex الرابط بيستخرج الـ ID صراحةً.
- 🔴 **شرط `UNFULFILLED` هو اللي بيمنع تحويل الإلغاء لـ RTO.** الأداة كانت
  بتقرا `displayFulfillmentStatus` من v2.0.0 و**ما بتستخدمهوش خالص**. حسب
  `ecommoda-order-lifecycle` قاعدة 2 الإلغاء بيتصنّف من حدث شوبيفاي:
  `cancelledAt + UNFULFILLED → CANCELLED` · `cancelledAt + FULFILLED → RTO`.
  وقاعدة 5 بتقول `Ready + Fulfilled` وضع شرعي (محاولة تسليم مُعادة)، يعني
  `manual_status` لوحده ماكانش بيمنع إلغاء أوردر متشحن. اتضاف في v2.3.0
  وبيتفحص في `lookup_order` وتاني في `cancel_order`.
  ⚠️ **الحالات `ON_HOLD` و`SCHEDULED` مرفوضة كمان — قرار مقفول 01-09-2026.**
  المتجر مابيستخدمهمش خالص (قرار أحمد، ومتأكَّد بجرد: صفر أوردر في الحالتين).
  الشرط يفضل `UNFULFILLED` حرفيًا. أي توسيع للـ Set لازم يبقى قرار صريح مكتوب
  هنا، مش تعديل صامت.
- ✅ **باج رفرفة الهيدر عند نقطة القفل — اتقفل في الواجهة v1.8.0.**
  النمط القديم كان عتبتين برقمين ثابتين (`const STICK_ON = 40, STICK_OFF = 12`).
  المشكلة إن قفل الهيدر **بيقلّل ارتفاعه فوق منطقة العرض**، والمتصفح
  (scroll anchoring) بيحافظ على المحتوى مكانه فبيطرح فرق الارتفاع من `scrollY`
  نفسه — فالفعل بيحرّك شرطه: `y=42 → يقفل → scrollY يرتد لـ ~0 → يفتح → يرجع 42`.
  والفرق ده أوسع من الباند (28px)، فالـ `requestAnimationFrame` كان بيبطّأ اللوب
  مش بيمنعه. دلوقتي `measureSticky()` بتقيس الفرق الفعلي و
  `STICK_ON = stickyDelta + STICK_OFF + 24`.
  ⚠️ **ممنوع الرجوع لرقم ثابت** — الفرق بيختلف مع أي تعديل على الهيدر أو الوصف
  أو شريط التابات. وإعادة القياس على تغيّر **العرض** بس وبـ debounce: الربط
  بالـ `resize` الخام بيرفرف على الموبايل لأن القياس نفسه بيلمس الكلاس.
- ✅ **قوائم الفلاتر كانت بتتقفل بعد أول اختيار — اتقفل في الواجهة v1.8.0.**
  `msRenderList` بتعيد بناء الـ `innerHTML` جوّه الـ inline `onclick`، فالعنصر
  المضغوط بيتشال من الـ DOM **قبل** ما listener الإغلاق يشوفه، و`contains()` على
  عنصر detached بترجّع `false` → الكود يفتكر الضغطة برّه ويقفل. الحارس
  `if (!document.contains(e.target)) return;` أول الـ listener **إلزامي**.
  ⚠️ الأثر الحقيقي مش سلوكي بس: فلتر صعب الاستخدام بيدفع الموظف يكتفي بقيمة
  واحدة — يعني **بند ٢١ (multi-select) بيتخالف من غير أي رسالة خطأ**.
- ⚠️ **`assertEnv(env, "shopify")` بيتنادى قبل أي مسار بيلمس شوبيفاي** (من
  Worker v2.9.0). غياب `SHOP_DOMAIN` من غيره بيدّي
  `"error code: 1003" is not valid JSON` — رسالة مابتدلش على السبب خالص. أي
  متغيّر جديد الـ Worker يعتمد عليه لازم يتضاف في `ENV_REQUIRED`، وإلا الفحص
  بيعدّي عليه ساكت.
- ⚠️ **حدود فلتر التاريخ في السجل بتوقيت UTC** زي ما `timestamp` متخزّن في D1،
  والعرض بتوقيت القاهرة (UTC+3). فرق التلات ساعات ممكن يخلي عملية بعد ٩ مساءً
  بتوقيت القاهرة تقع في يوم UTC اللي بعده. مقبول لفلتر بالأيام — مكتوب هنا
  عشان مايتكتشفش كباج بعدين.
- ⚠️ **`custom.cancel_manual_reason` حالة حالية مش سجل — بيتكتب فوقه.**
  على `#53033`: الأداة كتبت **"تأجيل غير محدد"** (`createdAt = 07:51:25Z`)،
  والقيمة دلوقتي **"السعر غالي"** (`updatedAt = 08:07:09Z`) — **أحمد غيّرها
  يدويًا من داشبورد شوبيفاي**، مش عطل ولا مصدر مجهول.
  بس النتيجة العملية باقية: **D1 هو المصدر الدائم** لأي تحليل لأسباب الإلغاء،
  والميتافيلد بيقول الحالة الحالية بس. الاتنين **مش بدائل لبعض** — وده يصحّح
  صياغة أقدم في الملف ده كانت بتساويهم.
- ⚠️ **"أوردر ملغي" ≠ "اتلغى بالأداة دي".** الإلغاء اليدوي من داشبورد شوبيفاي
  شغّال وموجود وأحمد عارف بيه — يوم 01-09-2026 كان فيه إلغاءات تانية
  (`#53034` · `#53023` · `#53008` · `#52982`) بنفس بصمة شوبيفاي بالظبط
  (`cancelReason: OTHER` + `cancel_manual_reason` مليان) و**مفيش ليها ولا صف
  في D1**. يعني بصمة شوبيفاي لوحدها مش كافية تنسب إلغاء للأداة؛ الإسناد من
  D1 بس.
- ⚠️ **حالة S1 بتتكتب بعد التأكيد بس.** من v2.6.0 الأداة بتحدّث
  `custom.manual_status` لـ `Cancelled`، بس **مش** لو الإلغاء رجع أصفر
  (مش مؤكَّد) — لأن "S1 = Cancelled" لازم توصف إلغاء حصل فعلاً، ووسم أوردر
  لسه حي أسوأ من تأخير الوسم. الفجوة دي مغطّاة عمليًا: فيه **Flow على شوبيفاي
  بيكتب نفس القيمة بعد ~15 ثانية** (متأكَّد على `#53033`: `cancelledAt`
  07:51:27Z و`manual_status.updatedAt` 07:51:42Z). يعني دلوقتي **كاتبان**
  لنفس القيمة — مفيش تعارض (نفس النص)، والفرق إن كتابة الأداة **فورية
  ومتسجّلة في D1** وكتابة الـ Flow لأ.
  ⚠️ لو الـ Flow اتقفل يومًا، الحالة الصفرا بتفضل من غير S1 — راجعها يدويًا.
- ⚠️ **فشل تحديث S1 مايلغيش إن الأوردر اتلغى.** الإلغاء لا رجعة فيه، فالنتيجة
  بتبقى ⚠ أصفر وسطر "ما تم فعليًا" بيفصّل: الإلغاء تم · السبب اتكتب · الحالة
  فشلت. ممنوع تتحسب فشل كامل.
- 🔴 **الـ Build watch paths متضيّقة — أي ملف جديد يعتمد عليه الـ Worker لازم
  يتضاف.** الـ Include paths دلوقتي `index.js` + `wrangler.toml` بس. لو الأداة
  كبرت وضافت `package.json` أو فولدر `src/` أو أي ملف الـ Worker بيعتمد عليه،
  **لازم يتضاف للقائمة** — وإلا الـ Worker هيفضل على نسخته القديمة **من غير
  أي رسالة ولا بناء فاشل**. نفس شكل الفخ الخامس في سكيل النقل بالظبط.
  ⚠️ والحقل **chips مش نص**: كل مسار لوحده + Enter. لو اتكتبوا بمسافة في
  صندوق واحد، بيتسجّل chip باسم مش موجود → **مفيش أي push هيبني تاني للأبد**.
- 🔴 **الثيم الأحمر استثناء مقصود — مش سهو ولا نسخة قديمة.** الأداة دي **تاني**
  استثناء في الستاك بعد `Bosta Warehouse Return` (قرار أحمد 01-09-2026). السبب
  تشغيلي: دي الأداة الوحيدة اللي بتعمل **فعل لا رجعة فيه** على أوردر حقيقي،
  والموظف بيقفز بينها وبين أدوات شكلها متطابق — اللون بيقوله "إنت في أداة
  الإلغاء" قبل ما يبص على العنوان.
  **طريقة التطبيق = المنصوص عليها في `ecommoda-html-builder` →
  `design-system.md`: override للـ `--accent` / `--accent-light` /
  `--accent-dark` بس.** `--red` و`--red-light` ما اتغيّروش، فبانرات الفشل
  والتحذير لسه بلونها المعياري.
  ⚠️ **زرار الإلغاء اتميّز بإطار غامق (`#7f1d1d`) مش بلون تاني** — بعد ما الثيم
  كله بقى أحمر، `--red` لوحده مابقاش بيفرّق الفعل الخطر عن أي زرار عادي. أي
  تعديل على `.btn-danger` لازم يحافظ على التمييز ده.
  ⚠️ **قاعدة ٢٥ في `ecommoda-html-builder` لسه بتقول "استثناء وحيد"** — البند ٧
  في `SKILL-FIXES.md` بيصحّحها. لحد ما يتقفل، أي مراجعة للأداة دي هتقرا الثيم
  كمخالفة وهو قرار موثّق.
- ⚠️ **فلتر سبب الإلغاء بيقرا من D1 مش من شوبيفاي — وده مقصود.**
  `get_log_reasons` بيرجّع `DISTINCT json_extract(extra,'$.reasonLabel')` من
  صفوف `order_cancel`. لو اتقرا من شوبيفاي (زي قائمة الأسباب في شاشة الإلغاء)
  كان هيحصل الاتنين دول في السكوت: سبب اتشال من تعريف الميتافيلد يبقى **غير
  قابل للفلترة** رغم إن صفوفه لسه في السجل، وسبب جديد لسه ما اتستخدمش يبقى
  خيار بيدّي **صفر نتايج دايمًا**.
  ⚠️ القايمة **مش متفلترة بباقي الفلاتر** — زي قايمة الموظفين بالظبط. لو
  اتفلترت، الخيارات بتختفي من تحت إيد الموظف وهو بيفلتر.
  ⚠️ **الفاصل بين القيم فاصلة (`,`) في الطرفين.** مفيش سبب حالي فيه فاصلة
  (متأكَّد من D1 01-09-2026)، ولو اتضاف واحد لازم الفاصل يتغيّر في الواجهة
  والـ Worker **مع بعض** — تغييره في مكان واحد = صفر نتايج بلا أي خطأ.
- ⚠️ **خطوة النتيجة بتاخد الأخضر والأصفر مع بعض — والأحمر بيفضل مكانه.**
  بعد إلغاء اتبعت فعلاً (✓ أو ⚠)، البحث وبطاقة الأوردر بيختفوا وتفضل النتيجة
  لوحدها، والرجوع بزرار **🔄 إلغاء أوردر آخر؟** بس. الأصفر بيروح لنفس الخطوة
  **عن قصد**: الفعل اتنفّذ ولا رجعة فيه، اللي ما اتأكدش هو التأكيد بس — ورجوع
  الموظف لبطاقة الأوردر بعده بيغري بمحاولة تانية على أوردر خلاص اتلغى. الفشل
  (✗) بس هو اللي بيفضل في البطاقة عشان يعيد المحاولة.
  ⚠️ **`currentOrder` بيفضل موجود طول ما إنت في خطوة النتيجة** — زرار "تحقق من
  شوبيفاي الآن" بيحتاجه. التصفير بيحصل في `resetToSearch()` بس (الزرار أو
  الـ logout). أي تصفير أبكر بيكسر زرار التحقق في السكوت.
- ⚠️ **سقف التصدير 2000 صف — والقصّ بيتقال صراحةً.** لو المطابق للفلاتر أكتر،
  الملف بيتقص، وبانر ثابت فوق الجدول بيقول اتصدّر كام من أصل كام. الرقم جاي من
  الـ Worker (`cap` · `total` · `truncated`) مش متخمّن في الواجهة — عشان
  مايتغيّرش في مكان ويفضل قديم في التاني. قبل v2.7.0 كان بيتقص في السكوت
  والرسالة "تم تصدير 2000 عملية ✓" على ملف ناقص.
- ⚠️ **قائمة الأسباب بتتقرا لايف من شوبيفاي** (`custom.cancel_manual_reason`
  choices). لو تعريف الميتافيلد اتمسح أو اتغيّر، `lookup_order` بيرجّع **502**
  ويوقف العملية — ده مقصود، مش عطل.
- ⚠️ **الإلغاء لا رجعة فيه.** `orderCancel` بترجّع `job` غير متزامن، فالأداة
  بتعيد قراءة الأوردر وتتأكد من `cancelledAt` قبل ما تقول "تم". لو ما اتأكدش،
  بترجّع `confirmed:false` والواجهة بتعرض ⚠️ أصفر مش ✓ أخضر.
- 🔴 **التحقق كان موجود من غير صبر — وده خلّى الأصفر هو الحالة الافتراضية.**
  لحد v2.3.0 القراءة البعدية كانت **فورية** (بعد أجزاء من الثانية من الميوتيشن)،
  والـ Job بيبدأ ينفّذ بعدها — فـ `cancelledAt` يبقى `null` و**كل إلغاء ناجح كان
  بيظهر أصفر**. اتأكد بالتجربة الأولى على `#53032` يوم 01-09-2026. اتصلح في
  v2.4.0: `waitForCancelConfirmation` بتستنى `job(id){done}` بـ backoff متصاعد
  400→2200ms (≈٦ ثوانٍ بحد أقصى) وبتوقف أول ما التأكيد يحصل.
  ⚠️ الأصفر **مالوش يتشال**: بعد الـ٦ ثوانٍ معناه "ما قدرناش نتأكد" — مش "تم"
  ومش "فشل". الواجهة بتدي زرار **🔄 تحقق من شوبيفاي الآن** بيعيد القراءة
  ويحوّله لأخضر لو اتأكد، وعمره ما بيحوّله لأحمر (الـ Job ممكن يكون لسه شغال).

## استرجاع النسخ القديمة

```
مفيش نسخ مرقّمة في الريبو — الريبو اتعمل نضيف من أول commit،
والواجهة اتنشرت لأول مرة هنا.
نسخة الـ Worker القديمة (v1.0.3) اللي كانت منشورة يدويًا على كلاودفلير
مش محفوظة في git — اتسحبت عبر MCP يوم 01-09-2026 للمقارنة بس.
```

## بصمة المهارات

| المهارة | الإصدار وقت آخر تعديل | كانت |
|---|---|---|
| ecommoda-worker-builder | v2.0.0 | v1.1.0 |
| ecommoda-html-builder | v6.0.0 | v4.0.0 |
| ecommoda-constants | v1.4.3 | v1.4.2 |
| shopify-graphql-helper | v1.0.0 | — |
| ecommoda-order-lifecycle | v1.2.0 | v1.1.0 |

آخر مطابقة: 01-09-2026 · `index.js` v2.9.0 · `index.html` v1.8.0
🔴 معلّقة: — لا شيء

**البنود الكاسرة اللي اتقفلت في المطابقة دي:**

| المهارة | البند 🔴 | إزاي اتقفل |
|---|---|---|
| worker-builder v2.0.0 | التحقق بعد ميوتيشن غير متزامنة لازم يستنى | كان **منفَّذ أصلاً** — الأداة هي مصدر البند. اتفكّ لـ `waitForJobConfirmation` العامة + غلاف محلي، بنفس السلوك بالحرف |
| worker-builder v2.0.0 | `get_logs_export` يرجّع `cap`/`total`/`truncated` | كان منفَّذ من v2.7.0 — والكتلة اتزامنت مع النسخة القياسية الجديدة |
| html-builder v5.0.0 | تصدير الـ Log Tab بيتقص في السكوت | البانر كان موجود من v1.6.0 — اتسمّى `logExportWarn` (الاسم القياسي) والرقم `2000` اتشال من نص الواجهة |
| **html-builder v6.0.0** | **عتبات قفل الهيدر تتقاس مش تتكتب** | **كان الباج موجود فعلاً** (`const STICK_ON = 40`) — اتصلح بـ `measureSticky()` |
| html-builder v3.0.0 | `createdAt.slice()` بتاريخ UTC | مالوش أثر — الأداة مافيهاش `monthChunks` ولا `visibleRows` |
| order-lifecycle v1.2.0 | (🟡) G-14 — `PARTIALLY_FULFILLED` | صفر أثر: الأداة بترفض أي حاجة غير `UNFULFILLED` أصلاً، فمستحيل تلغي أوردر مشحون جزئيًا

## مسائل مفتوحة

1. ~~`cancel_failed` في `ecommoda-constants` §7~~ · 2. ~~مراجعة `REASON_ENUM_MAP`~~ ·
   3. ~~زرار اختلاف النسخة~~ · 4. ~~تضييق Build watch paths~~ · 5. ~~خط الأساس~~ ·
   5-ب. ~~امتداد فلاتر السجل~~ · 6. ~~`ON_HOLD` / `SCHEDULED`~~
   — كلها اتقفلت أو اتنقلت للبند الوحيد تحت. التفاصيل في «سجل البنود المقفولة».

### 🟡 اللي فاضل من تعديلات المهارات

**جلسة تعديل المهارات اتنفّذت** — ٦ من الـ٧ بنود في `SKILL-FIXES.md` بقوا
في المهارات فعلاً (`worker-builder` v2.0.0 · `html-builder` v5.0.0/v6.0.0 ·
`constants` v1.4.3 · `order-lifecycle` v1.2.0)، والأداة اتزامنت معاهم في
Worker v2.9.0 والواجهة v1.8.0. الجدول الكامل بالحالة → `SKILL-FIXES.md`.

**الباقي مفتوح — بندان:**

| # | البند | الحالة |
|---|---|---|
| ٧ | قاعدة الثيم في `ecommoda-html-builder` لسه بتقول «استثناء وحيد» | 🔴 مفتوح — أي مراجعة للأداة دي هتقرا الثيم الأحمر كمخالفة وهو قرار موثّق |
| ٣-ب | نص البند ٣: الفلترة بحقل متخزّن جوّه `extra` (`reasons` + `json_extract`) | 🟡 مفتوح — المهارة تبنّت `employees[]`/`types[]` بس |

✅ **البند اللي كان مسجّل هنا لوحده اتقفل:** `cancel_failed` بقت مسجّلة في
`ecommoda-constants` §7 من v1.4.3، ومعاها `metafields_change` بقى ليه جدول
كُتّاب. يعني الأداة **مابقتش** بتكتب قيمة `type` مش موجودة في جدول الثوابت.

⚠️ **الحاجة الوحيدة الباقية خارج المعيار:** باراميتر `reasons` في دوال السجل —
امتداد محلي على `§SHARED` **مقصود وموثّق**، ومعلّم صراحةً جوّه الكود. باقي
الكتلة بقت مطابقة للنسخة القياسية بالحرف بعد worker-builder v2.0.0.

### ✅ سجل البنود المقفولة (للتأريخ)

| # | البند | اتقفل إزاي |
|---|---|---|
| 2 | مراجعة `REASON_ENUM_MAP` | الجدول اتشال بالكامل والسبب بقى `OTHER` ثابت |
| 3 | زرار اختلاف النسخة | `ecommoda-html-builder` v4.0.0 — `MIN_WORKER_VERSION` + `cmpVersion()` |
| 4 | تضييق Build watch paths | اتعملت في الداشبورد: `index.js` + `wrangler.toml` **صندوقين منفصلين** — والاختبارين (السلبي مع دمج `.md` بس · الإيجابي مع دمج PR #3 اللي لمس `index.js`) عدّوا الاتنين |
| 5 | خط الأساس | اتكتب من D1 + شوبيفاي بعد أول تشغيل حقيقي |
| 6 | `ON_HOLD` / `SCHEDULED` | قرار أحمد: المتجر مابيستخدمهمش — `UNFULFILLED` حرفيًا بدون تعديل |

**التحقق المتبقّي على بند ٤ — خطوة واحدة في الداشبورد:**
سكيل النقل §13-ب بيفرض اختبارين، والسلبي منهم **بيتنفّذ تلقائيًا مع الدمج ده**
لأنه تعديل `.md` بس. بعد الدمج افتح **Deployments** واتأكد إن `f6b1b5e4` لسه
هو الـ **Active** ومفيش نسخة جديدة اتعملت — كده الاختبار السلبي عدّى.
~~الاختبار الإيجابي بيعدّي تلقائيًا مع **أول تعديل حقيقي على `index.js`**~~ —
✅ **عدّى 01-09-2026** مع دمج PR #3 (Worker v2.8.0): التعديل لمس `index.js`،
البناء اشتغل، والكود المنشور اتقرا عبر MCP وفيه `WORKER_VERSION = "2.8.0"`.
**بند ٤ اتقفل بالكامل — الاختبارين السلبي والإيجابي الاتنين عدّوا.**

آخر تحديث: 02-09-2026 — سبب الإلغاء بقى يتختار بـ chips بدل الـ dropdown (الواجهة v1.9.0)

</div>
